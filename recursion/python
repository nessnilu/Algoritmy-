def power(x, n):
    if n == 0:
        return 1
    if n < 0:
        return 1 / power(x, -n)
    return x * power(x, n - 1)
print(power(2, 3))   
print(power(5, 0))   
print(power(2, -2))  

#вывод
#8
#1
#0.25
